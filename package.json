{
  "name": "books",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4",
    "@sap/cds-hana": "^2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "start": "cds watch",
    "dev": "cds run --profile dev",
    "build": "cds build"
  },
  "cds":{
    "requires": {
      "db": {
        "[dev]": { "kind": "sql"},
        "[prod]": { "kind": "hana-cloud"}
      }
    }
  }
}
